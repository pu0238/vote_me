service : (vec principal) -> {
  activate_user : (principal, text) -> ();
  create_vote : () -> ();
  register_new_entry_identities : (vec principal) -> ();
  user_belongs_to_committee : () -> (bool) query;
}
